interface:
  display_name: "Telegram Platform Adapter"
  short_description: "Repo-specific patterns and workflows for telegram-platform-adapter"
  default_prompt: "Use the telegram-platform-adapter repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true